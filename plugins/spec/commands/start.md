---
description: Start a spec — interview the user and produce validated requirements
argument-hint: "[brief description of the feature or topic]"
---

$ARGUMENTS

Load and execute the `spec:start` skill with the arguments above.
